# GHA-Dynamics — Power Platform CI/CD Pipeline

Enterprise-grade GitHub Actions pipeline for Microsoft Power Platform. Thin caller repo that delegates all build and deploy logic to [GHA-Core](https://github.com/ppudot2-cloud/GHA-Core).

---

## Two-Pipeline Architecture

**Pipeline 1 — `build-and-deploy.yml`**
Triggered by PR or dispatch. Export → Build (per solution, parallel) → Deploy Dev / Intg / UAT / FRS / Perf (all parallel) → Create PR to main.

**Pipeline 2 — `deploy-prod.yml`**
Triggered automatically when the Pipeline 1 PR merges to main. Reads `pipeline-context.json` → Deploy UAT (re-validation gate) → Deploy Prod.

```
Pipeline 1:  PR/dispatch → Export → Build × N → Dev ┐
                                                Intg │ parallel
                                                UAT  ├ → create PR → main
                                                FRS  │
                                                Perf ┘

Pipeline 2:  PR merge → read context → UAT gate → Deploy UAT → Prod gate → Deploy Prod
```

---

## Repository Structure

```
GHA-Dynamics/
├── .github/
│   ├── workflows/
│   │   ├── build-and-deploy.yml    # Pipeline 1
│   │   ├── deploy-prod.yml         # Pipeline 2
│   │   ├── export-solution.yml     # Export from sandbox
│   │   ├── pr-validation.yml       # Build-only PR check
│   │   └── rollback.yml            # Environment rollback
│   └── config/
│       └── project-vars.yml        # Project-specific variable overrides
├── solutions.json                  # Solution registry — name, folder, deploy order
├── pipeline-context.json           # Cross-pipeline handoff — auto-generated by Pipeline 1, not committed to repo
├── src/solutions/{Name}/           # Unpacked solution source
├── config/{Name}/data-schema.xml   # Config migration schema (optional)
├── deployment-settings/
│   └── {env}/{Name}.json           # Per-environment variable overrides
├── docs/                           # All documentation
│   ├── gha_cicd_e2e_flow.html          # Interactive E2E flow diagram
│   ├── PIPELINE_REFERENCE.md           # Full reference for all components
│   ├── ENTERPRISE_DEVSECOPS_GUIDE.md   # Enterprise setup guide
│   ├── QUICK_START.md                  # First pipeline run guide
│   └── SECRETS_SETUP_GUIDE.md          # Secrets & variables reference
└── scripts/
    └── simulate-pipeline.py        # Local dry-run simulation
```

---

## Quick Start

1. Set up Azure OIDC + Key Vault → see [docs/ENTERPRISE_DEVSECOPS_GUIDE.md](docs/ENTERPRISE_DEVSECOPS_GUIDE.md)
2. Configure GitHub environments, secret, and variables → see [docs/QUICK_START.md](docs/QUICK_START.md)
3. Edit `solutions.json` with your solution names
4. Run `build-and-deploy.yml` with `mock_deploy: true` to validate the wiring
5. Merge the created PR → Pipeline 2 fires automatically

---

## Documentation

| Document | Description |
|---|---|
| [gha_cicd_e2e_flow.html](docs/gha_cicd_e2e_flow.html) | Interactive diagram of the complete pipeline flow |
| [PIPELINE_REFERENCE.md](docs/PIPELINE_REFERENCE.md) | Every workflow, action, script, and config file explained |
| [ENTERPRISE_DEVSECOPS_GUIDE.md](docs/ENTERPRISE_DEVSECOPS_GUIDE.md) | Azure setup, OIDC, Key Vault, federated credentials |
| [QUICK_START.md](docs/QUICK_START.md) | Step-by-step first pipeline run |
| [SECRETS_SETUP_GUIDE.md](docs/SECRETS_SETUP_GUIDE.md) | GitHub secrets and variables reference |

---

## Key Design Decisions

**Sequential imports within each environment** — Dataverse cannot process parallel solution imports to the same environment. `deploy-all-solutions` always imports solutions one at a time in `deployOrder` sequence.

**Parallel across environments** — Pipeline 1 deploys Dev, Intg, UAT, FRS, and Perf simultaneously. Each environment is independent and can proceed without waiting for others.

**Zero long-lived secrets in GitHub** — Only `GHA_CORE_PAT` is stored as a GitHub Secret. All Power Platform credentials are fetched at runtime from Azure Key Vault via OIDC.

**Mock deploy** — Set `mock_deploy: true` to simulate the entire pipeline (build, Solution Checker, deploy) without connecting to Dataverse, Azure, or JFrog. Every step runs; nothing is imported.
